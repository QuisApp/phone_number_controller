name: phone_number_controller
description: A TextEditingController formatting international phone numbers as you type them.
version: 1.0.2
homepage: https://github.com/QuisApp/phone_number_controller

environment:
  sdk: ">=3.1.3 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  libphonenumber: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
